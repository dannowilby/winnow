# Minimal single-binary Loki for local development, with native OTLP log
# ingestion enabled (the collector pushes logs to /otlp).

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2020-10-24
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  # Required so OTLP resource/scope attributes can be stored as structured
  # metadata on each log line.
  allow_structured_metadata: true
  volume_enabled: true
