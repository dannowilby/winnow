
pub fn run_job(folder: &str) {
    println!("Creating and running job for folder: {folder}");

    // this node becomes the master

    // partitions data into chunks and assigns map tasks to workers
}